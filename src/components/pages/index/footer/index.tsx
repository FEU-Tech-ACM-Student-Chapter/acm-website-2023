import styles from "./index.module.css";
const Footer: React.FC = () => {
	return (
		<footer className={styles.ctr}>
			<span className={styles.linksCtr}></span>
		</footer>
	);
};

export default Footer;
